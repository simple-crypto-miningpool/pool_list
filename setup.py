#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='pool-list',
      version='0.1.0',
      description='A website for tracking different pool stats',
      author='Isaac Cook',
      author_email='isaac@simpload.com',
      url='http://pools.simplevert.com',
      packages=find_packages()
      )
